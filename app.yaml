# Configuration file for deploying on Google App Engine
runtime: custom
env: flex
# entrypoint: uvicorn main:app

# runtime_config:
#   python_version: 3.7

# manual_scaling:
#   instances: 1
# resources:
#   cpu: 1
#   memory_gb: 0.5
#   disk_size_gb: 10

# network:
#   forwarded_ports:
#     - 8000/tcp

includes:
  - envs.yaml
